version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PUQ_AI_KEY=${PUQ_AI_KEY}
    volumes:
      - db_data:/app/db

volumes:
  db_data:
