version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PUQ_WEBHOOK_URL=${PUQ_WEBHOOK_URL}
    volumes:
      - db_data:/app/db

volumes:
  db_data:
